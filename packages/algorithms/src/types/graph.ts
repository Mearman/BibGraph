/**
 * Core graph algorithm types.
 *
 * Node and Edge interfaces have moved to @bibgraph/types package.
 * Re-exporting here for backwards compatibility.
 *
 * @deprecated Import Node and Edge from @bibgraph/types instead
 */

// Re-export from @bibgraph/types for backwards compatibility
export type { Node, Edge } from '@bibgraph/types';
