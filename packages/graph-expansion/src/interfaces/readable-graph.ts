/**
 * ReadableGraph - Minimal generic interface for graph traversal algorithms.
 *
 * This interface has moved to @bibgraph/types package.
 * Re-exporting here for backwards compatibility.
 *
 * @deprecated Import ReadableGraph from @bibgraph/types instead
 */

// Re-export from @bibgraph/types for backwards compatibility
export type { ReadableGraph, NodeBase, EdgeBase } from '@bibgraph/types';
